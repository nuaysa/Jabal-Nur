/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['maps.app.goo.gl', 'maps.googleapis.com', 'lh3.googleusercontent.com'],
    },
};

export default nextConfig;
